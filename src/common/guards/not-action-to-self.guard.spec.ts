import { NotActionToSelfGuard } from './not-action-to-self.guard';

describe('NotActionToSelfGuard', () => {
  it('should be defined', () => {
    expect(new NotActionToSelfGuard()).toBeDefined();
  });
});
