import { PaginationQueryExceptionFilter } from './pagination-query-exception.filter';

describe('PaginationQueryExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PaginationQueryExceptionFilter()).toBeDefined();
  });
});
